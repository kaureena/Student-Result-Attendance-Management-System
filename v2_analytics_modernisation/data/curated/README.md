# Curated outputs (Warehouse-friendly)

ETL generates curated fact tables as CSV:
- `fact_attendance.csv`
- `fact_results.csv`

These are also loaded into the local SQLite warehouse DB: `warehouse/warehouse.db`

# API Service (Optional)

FastAPI template that exposes simple read endpoints from the OLTP dataset.

Run (after installing v2 requirements):
```bash
uvicorn api_service.app.main:app --reload --port 8000
```

# Health checks (what to verify)

- API starts successfully and `/docs` loads
- ETL smoke run completes without errors
- DQ checks pass (or failures are recorded in the DQ issue register)
- Power BI refresh works against the curated outputs (sample data)

# --- Database (OLTP) ---
# For local demo, v1 uses SQLite file in v1_bca_basic_system/database/local.db
# For v2, you can point to PostgreSQL if available.
OLTP_DB_TYPE=sqlite
OLTP_SQLITE_PATH=./v1_bca_basic_system/database/local.db

# Example PostgreSQL (optional)
# OLTP_DB_TYPE=postgres
# OLTP_PG_HOST=localhost
# OLTP_PG_PORT=5432
# OLTP_PG_DB=student_oltp
# OLTP_PG_USER=postgres
# OLTP_PG_PASSWORD=postgres

# --- Warehouse (OLAP) ---
WH_DB_TYPE=sqlite
WH_SQLITE_PATH=./v2_analytics_modernisation/warehouse/warehouse.db

# --- Runs / Logging ---
RUN_ID_PREFIX=run
LOG_LEVEL=INFO

# --- API (optional) ---
API_PORT=8000

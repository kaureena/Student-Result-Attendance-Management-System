# v1 — BCA Basic System

A simple baseline application (portfolio demo) for:
- Student records
- Attendance entry
- Results entry
- Export to Excel

## Run
```bash
python -m pip install -r requirements.txt
python src/main.py
```

Default login:
- username: `admin`
- password: `admin`

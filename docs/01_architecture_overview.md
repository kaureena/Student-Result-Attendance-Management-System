# Architecture Overview

- **v1 (BCA baseline):** basic app writes to a small operational database.
- **v2 (modernisation):** ETL loads reporting tables (star schema) and feeds Power BI.

See Mermaid dashboards under `docs/mermaid/`.

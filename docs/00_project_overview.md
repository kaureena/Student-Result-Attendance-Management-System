# Project Overview

## Goal
Build a simple student result & attendance system (**v1**) and modernise it into a BI-ready analytics platform (**v2**).

## What this demonstrates
- Data modelling (OLTP + Star schema warehouse)
- ETL pipeline (extract → transform → load)
- Data quality checks and reporting
- Dashboard planning (Power BI) and SharePoint embedding workflow
